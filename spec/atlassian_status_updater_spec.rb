RSpec.describe Asu do
  it "has a version number" do
    expect(Asu::VERSION).not_to be nil
  end

  it "does something useful .. we'll add tests for this later (maybe)" do
    expect(false).to eq(false)
  end
end
